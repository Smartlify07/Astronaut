const main = document.querySelector('main');
const mainBg = document.querySelector('main').style.backgroundImage;

const nextBtn = document.querySelector('.next');
nextBtn.addEventListener('click', () => {
  setTimeout(() => {}, 2000);
  //   main.style.backgroundImage = 'url(../images/astro2.webp)';
});
